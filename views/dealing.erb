<h1> Outcome </h1>
(The card values are Jack = 11, Queen = 12, King = 13)
<br>
<br>
<% @transformed_record.each { |record_hash| %>
  <% record_hash.each {| player, hand| %>
    <%= player %> :
    <% hand.each {|card| %>
      <%  card.each {|suit, number| %>
      <%= number %> of <%= suit  %> ||
      <% } %>
    <%    } %> <br>
  <% } %>
<% } %>

<br>

<%= @statement %>
